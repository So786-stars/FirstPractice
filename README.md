# FirstPractice
This is first Repository that i create to known how the GitHub works

Now : -
This i the changes that i do from vscode after cloning the repositry/folder
from Github via using this command and links (git clone https://github.com/So786-stars/FirstPractice.git)


Now : - 
After saving the README.MD/current_file the status of file is M thats means 
Modification status now ready to save 
So : -
to Check the status we use this command (git status) 
if status is in modification stage or untracked stage means U  then we used command (git add file_name) to save the changes/modification that we do in current file or newfile.


Now : -
if we use command like (git add file_name)/ (git add .)  to save the modification then M is 
display a A means Add the modification file to current_file 


Now : -
we are going to commit/save the file using this command (git commit -m "some meaningful  msg") after modification and addition in files 
Steps : -
create newfile/existing files : modification -> addition(to git file) -> commmit(save the additional changes)

::: git status -->  
::: git add .  --> 
::: git comit -m "msg"  -->
::: git push origin main -- > FINALLY THE CHANGES IS UPLOADED LOCAL SYSTEM TO REMOTE(GITHUB)
 